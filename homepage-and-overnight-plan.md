# תוכנית דף הבית + משימת הלילה (גרסת צוות 5 סוכנים + סוכן ראשי)

נבנה ע"י: אסטרטג SEO, מומחה GEO/AI, מהנדס ביצועים, מהנדס בטיחות WP, ארכיטקט משימות + סוכן ראשי מכריע.
ביצוע: Claude בכרום, ללא השגחה (הבעלים לא זמין עד למחרת).

## הכרעות הסוכן הראשי
1. Delay JS — נדחה לסשן מפוקח (שבירת tel/WhatsApp בלילה = אובדן שיחות). Defer JS רגיל מותר.
2. Title סופי: `החלפת מצבר לרכב עד הבית 24/7 כולל שבת | החל מ-379 ₪ | מצבר הדרך`
   Meta סופי: `החלפת מצבר לרכב עד הבית 24/7 כולל שבת, החל מ-379 ₪. טכנאי מגיע אליכם, דירוג 4.9 מ-108 ביקורות. מצבר הדרך - חייגו: 055-5033335`
3. llms.txt — דילוג (אין דרך בטוחה בלי SSH/תוסף). לסשן SSH הבא.
4. GEO Box אחרי H1 — מותר כווידג'ט חדש בלבד + Elementor History rollback; אם ההירו תבנית גלובלית — fallback לסקשן שמתחת; בספק — דלג.
5. סדר: דף הבית קודם, ואז משימת הלילה. תקרת 3 שעות ל-alt-text. smoke-test לפני הדוח.
6. תיקונים: WebP רק בלי .htaccess; Speakable רק אם TSF לא מוציא WebPage; ניקוי קאש URL בודד אחרי כל שינוי בדף הבית; alt-text עם dry-run של 10 ראשונות; font preload רק self-hosted; בדיקת מחשבון אחרי כל שינוי Rocket; שאריות עובדתיות מחוץ לדף הבית — סימון בלבד.

## חלק א — דף הבית (SEO+GEO+מהירות)
1. אבחון מלא (H1, title/meta נוכחיים, FAQ גלוי, סכמות קיימות, קישורים יוצאים, PSI בסיס 3x).
2. עדכון TSF title+meta (אחרי תיעוד הקיים).
3. GEO Box ציטוטי אחרי H1 (id=geo-summary, ווידג'ט חדש, טקסט קבוע ללא מקפים ארוכים, לאמת שהדירוג 4.9/108 תואם לנראה).
4. FAQPage: בדיקת כפילות קודם; אם אין — סקריפט יחיד בווידג'ט HTML עם כל השאלות הגלויות מילה במילה (טקסט נקי).
5. Speakable: רק אם אין WebPage קיים מ-TSF; אחרת דילוג + תיעוד.
6. סקשן קישורים פנימיים חדש בתחתית: 8 ערים + 5 יצרנים + מחשבון + 2-3 בלוג (slugs מהקטלוג המאומת).
7. מהירות (ללא Delay JS): מידות תמונות -> lazy-load עם החרגת hero -> WebP (תנאי) -> Defer JS -> minify CSS. בדיקה פונקציונלית מלאה אחרי כל אחד (tel/WhatsApp/טופס/jet-toggle/מחשבון/הדר דביק/קונסול) + revert מיידי.
8. אימות סופי: Rich Results, PSI אחרי, H1 יחיד, אפס כפילויות סכמה.

## חלק ב — משימת הלילה (9 שלבים, ~10 שעות)
1. GSC (קריאה): Coverage, שאילתות Top-200, עמודים מובילים.
2. זחילת QA מלאה (קריאה): כל עמוד — H1/title/meta/סכמה/קישורים שבורים/שאריות עובדתיות (449, 24-6, 12-48; סימון בלבד)/תוכן דל. מועמדי noindex ב-accumulators — רשימה בלבד.
3. מיפוי קניבליזציית בלוג (קריאה).
4. מפת הזדמנויות קישור פנימי (קריאה).
5. אודיט עמודי קיבולת (קריאה).
6. אודיט /en/ מלא (קריאה) + 3 תרחישים להחלטת בעלים.
7. PSI ל-20 עמודים מובילים (קריאה).
8. Alt-text עברי לתמונות עם שדה ריק בלבד (כתיבה תוספתית, dry-run 10 ראשונות ביומן, תקרת 3 שעות, יומן מלא לשחזור).
9. ניקוי קאש מלא + smoke-test (טלפון/וואטסאפ/טופס/מחשבון/קונסול) + דוח מסכם: תקציר מנהלים, מה בוצע, Top-15 ממצאים (חומרה x מאמץ), נספחים, רשימת החלטות לבעלים, תקלות.

## חוקים קשיחים (שני החלקים)
אין: תוספים/תבניות/עדכונים, משתמשים, סיסמאות, Display Conditions, BSR בלי dry-run, מחיקות, פרמלינקים/wp-config/.htaccess/DNS, Remove Unused CSS, Combine/Minify JS, Delay JS, נגיעה בתוכן יצרנים, מקפים ארוכים.
סכמה: רק Text mode או HTML widget; FAQ בטקסט נקי (wpautop).
תנאי עצירה: 500/מסך לבן, שמירה נכשלת x2, בקשת סיסמה, אזהרת אבטחה, אי-התאמה, ספק -> תיעוד ודילוג.
יומן: [זמן] | עמוד | פעולה | לפני | אחרי | אימות | rollback.
קאש: URL בודד אחרי שינוי בדף הבית; מלא — שעתי/סוף אצווה.
