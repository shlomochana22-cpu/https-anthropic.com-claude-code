import { StatsDashboard } from "@/components/StatsDashboard";

// StatsDashboard loads the signed-in producer's own events client-side.
export default function StatsPage() {
  return <StatsDashboard />;
}
